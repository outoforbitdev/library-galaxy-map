# Images

> Place shared images here (e.g., screenshots, logos, UI mockups referenced by multiple documents).
>
> For feature-specific images, use the `assets/` folder within the relevant feature directory (e.g., `docs/internal/features/galaxy-map/assets/`).
