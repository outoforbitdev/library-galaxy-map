name: Release
on:
  push:
    branches: ["main"]

# Declare default permissions as read only.
permissions: read-all

jobs:
  release:
    # yamllint disable-line rule:line-length
    uses: outoforbitdev/reusable-workflows-library/.github/workflows/release.yml@6d3deb76a8bc67b0c56bfae3e58d9b9566a967d0 # v1.3.0
    permissions:
      contents: write
